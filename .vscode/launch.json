{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}/tmp/bin/main",
            "args": [],
            "preLaunchTask": "Fuck",
            "cwd": "${fileDirname}"
        }
    ]
}